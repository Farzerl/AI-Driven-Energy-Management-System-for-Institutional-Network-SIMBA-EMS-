# Git history note

The inventoried working folder did not contain `.git` metadata. This repository builder does not fabricate development history.

When the `-InitialiseGit` option is used, it creates one truthful release commit named:

`AI4I Track 3 adjudication source release`

Future changes should be committed normally with clear messages. Do not create artificial backdated commits.
